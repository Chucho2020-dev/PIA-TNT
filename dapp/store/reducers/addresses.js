const initialState = {
    managerAddress : "0x78614b4f7bfc84a76e2926cef1fd79c603a6a047"
}

const addresses = (state = initialState, action) => {
    switch(action.type) {
        default: 
            return state;
    }
}

export default addresses;